# Custom Boot File for C FFI

This repository demonstrates how to use a boot file in order to invoke `load-shared-object` such that programs and libraries can call `foreign-procedure` at the top-level.
